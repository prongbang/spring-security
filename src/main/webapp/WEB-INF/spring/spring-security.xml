<b:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:b="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/security 
	http://www.springframework.org/schema/security/spring-security.xsd">

	<!-- 
		- Migrating from Spring Security 3.x to 4.x (XML Configuration).
		- http://docs.spring.io/spring-security/site/migrate/current/3-to-4/html5/migrate-3-to-4-xml.html#m3to4-xmlnamespace-form-login
		- Spring Security Reference.
		- http://docs.spring.io/spring-security/site/docs/4.2.x/reference/htmlsingle/ 
	-->

	<global-method-security secured-annotations="enabled" pre-post-annotations="enabled"/>

	<http
		authentication-manager-ref="customAuthProvider"
		auto-config="true" 
		use-expressions="true">
        
        <intercept-url pattern="/" access="isAuthenticated()" />
        <intercept-url pattern="/admin**" access="hasRole('ADMIN')" />
        <intercept-url pattern="/dba**" access="hasRole('ADMIN') and hasRole('DBA')" />
        
        <form-login
		    login-page="/login"
		    login-processing-url="/j_spring_security_check"
		    default-target-url="/"
			authentication-failure-url="/login?error"
			username-parameter="j_username"
			password-parameter="j_password" />
		
		<logout
			invalidate-session="false"
			logout-url="/j_spring_security_logout" 
			logout-success-url="/login?logout" />
		
        <session-management
        	invalid-session-url="/login?expired"
        	session-authentication-error-url="/login?error" 
        	session-fixation-protection="newSession">
        	
    		<concurrency-control 
    			max-sessions="1" 
    			error-if-maximum-exceeded="true" 
    			expired-url="/login?expired" />
    		
		</session-management>
		
		<access-denied-handler error-page="/403" />
		
    </http>
	
	<!-- Custom Authentication Provider -->
	<authentication-manager id="customAuthProvider"> 
		<authentication-provider ref="customAuthenticationProvider" />
	</authentication-manager>
	
	<b:bean id="customAuthenticationProvider" class="com.prongbang.sec.security.authentication.impl.CustomAuthenticationProvider" />

</b:beans>